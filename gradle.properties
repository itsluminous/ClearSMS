org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
org.gradle.caching=true
org.gradle.parallel=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# AGP 8.7.3's bundled lint crashes (IncompatibleClassChangeError in
# NonNullableMutableLiveDataDetector) analyzing Kotlin 2.3.x sources. Run the
# newer lint tool that understands Kotlin 2.3 metadata while keeping AGP 8.7.3
# (AGP 9 is intentionally held back - see the deps/2026-09-sweep branch notes).
android.experimental.lint.version=8.13.2
